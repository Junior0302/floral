"use client";

import { memo } from "react";

/** Désactivé — fond vidéo + overlay uniquement (plus de BG plein écran par page) */
function PageBackground() {
  return null;
}

export default memo(PageBackground);
